# ─────────────────────────────────────────────────────────────────────────────
# TS2I IVS v7.0 — Dependencies (pinned)
# Rule-Governed Hierarchical Inspection System
# Generated from .venv/ pip freeze (Python 3.12.3)
# ─────────────────────────────────────────────────────────────────────────────
# Install :
#   python -m venv .venv && source .venv/bin/activate
#   pip install -U pip wheel
#   pip install -r requirements.txt
#
# Optional / hardware-only extras live at the bottom of this file.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core vision ──────────────────────────────────────────────────────────────
opencv-python-headless==4.13.0.92   # OpenCV runtime (headless = no GUI deps)
numpy==2.4.4
scipy==1.17.1
scikit-image==0.26.0
imageio==2.37.3
tifffile==2026.3.3
pillow==12.2.0

# ── AI / ML ──────────────────────────────────────────────────────────────────
scikit-learn==1.8.0
joblib==1.5.3
onnxruntime==1.25.0
protobuf==7.34.1
flatbuffers==25.12.19
threadpoolctl==3.6.0
networkx==3.6.1
lazy-loader==0.5

# ── Plotting / fonts (used by reporting + skimage) ───────────────────────────
matplotlib==3.10.9
contourpy==1.3.3
fonttools==4.62.1
kiwisolver==1.5.0
cycler==0.12.1
pyparsing==3.3.2

# ── Reporting (PDF) ──────────────────────────────────────────────────────────
weasyprint==68.1
pydyf==0.12.1
pyphen==0.17.2
tinycss2==1.5.1
tinyhtml5==2.1.0
cssselect2==0.9.0
webencodings==0.5.1
zopfli==0.4.1
brotli==1.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3

# ── UI (Qt) ──────────────────────────────────────────────────────────────────
PyQt6>=6.4.0
PyQt6-Qt6==6.11.0
PyQt6_sip==13.11.1

# ── Web API ──────────────────────────────────────────────────────────────────
fastapi==0.136.1
starlette==1.0.0
uvicorn==0.46.0
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
pydantic==2.13.3
pydantic_core==2.46.3
annotated-types==0.7.0
annotated-doc==0.0.4
typing-inspection==0.4.2
typing_extensions==4.15.0
python-multipart==0.0.26          # multipart/form-data uploads (POST /products/import)

# ── Auth (JWT) ───────────────────────────────────────────────────────────────
python-jose==3.5.0
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.3

# ── Config ───────────────────────────────────────────────────────────────────
PyYAML==6.0.3

# ── System monitoring (§18.4) ────────────────────────────────────────────────
psutil==7.2.2

# ── Misc runtime ─────────────────────────────────────────────────────────────
click==8.3.3
certifi==2026.4.22
idna==3.13
six==1.17.0
python-dateutil==2.9.0.post0
packaging==26.1
cffi==2.0.0
pycparser==3.0
Pygments==2.20.0

# ── Tests ────────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-qt==4.5.0
pluggy==1.6.0
iniconfig==2.3.0

# ── Lint ─────────────────────────────────────────────────────────────────────
ruff==0.15.12

# ─────────────────────────────────────────────────────────────────────────────
# Optional / not installed by default — uncomment per deployment
# ─────────────────────────────────────────────────────────────────────────────
# Barcode decoding (uses libzbar0 system pkg — see README)
# pyzbar>=0.1.9
#
# OCR (uses tesseract-ocr + libtesseract — see README)
# pytesseract>=0.3.10
#
# YOLO training / fine-tune (heavy — pulls torch). Inference uses onnxruntime.
# ultralytics>=8.1
#
# Color science (Delta E, illuminants D50/D65) — observer color avancé
# colormath>=3.0.0
#
# Auth bcrypt hashing (server) — required when core/auth.py is wired
# passlib[bcrypt]>=1.7.4
# bcrypt>=4.1.2
#
# Raspberry Pi 5 GPIO backend (pip install on the Pi only)
# RPi.GPIO>=0.7.1 ; platform_machine == "aarch64"
# lgpio>=0.2.2.0 ; platform_machine == "aarch64"
